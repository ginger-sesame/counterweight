# Counterweight implementation contracts

This tracked directory contains the shareable technical contracts and validation artifacts produced in Phase 0. Local orchestration checklists remain in ignored `docs/`; this directory does not publish those local notes or change their Git policy.

Start with [Phase 0](phase0/README.md). These documents specify a controlled MVP, not production deployment authorization. No strategy application is implemented here.
